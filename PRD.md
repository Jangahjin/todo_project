# PRD — Todo List 서비스

| 항목 | 내용 |
|---|---|
| 문서 버전 | 1.2 |
| 최초 작성일 | 2026-08-26 |
| 개정일 | 2026-08-27 (v1.2) |
| 상태 | 개발 착수 전 확정본 |
| 관련 문서 | [CLAUDE.md](../CLAUDE.md)(개발 규칙) · [ROADMAP.md](ROADMAP.md)(개발 순서) · [dev-tools.md](guides/dev-tools.md)(개발 도구) |

> 이 문서는 **무엇을 만들 것인가**를 정의한다. **어떤 순서로 만들 것인가**는 `ROADMAP.md`, **어떻게 만들 것인가**(버전, 컨벤션, 폴더 구조, 금지사항)는 `CLAUDE.md`가 담당한다. 세 문서가 충돌하면 기술 규칙은 `CLAUDE.md`, 개발 순서는 `ROADMAP.md`를 따른다.

---

## 1. 제품 개요

**한 줄 정의:** 개인 사용자가 자신의 할 일을 생성·정리·완료 처리할 수 있는 웹 기반 Todo 관리 서비스.

**배경:** 이메일 또는 구글 계정으로 로그인해 자신의 할 일만 안전하게 관리하는 것을 목표로 한다. 팀 협업, 공유, 알림 같은 기능은 이번 범위에 넣지 않고 개인 사용자의 기본 흐름을 완결성 있게 구현하는 데 집중한다.

**개발 방식:** Claude Code를 활용한 바이브 코딩. 프론트엔드·백엔드·문서를 **독립된 3개 git 저장소**로 분리해 관리한다(`CLAUDE.md` 1장). 로컬 환경에서 전부 개발한 뒤, 완성 후 AWS로 이전한다.

---

## 2. 목표와 비목표

목표는 `ROADMAP.md` 2장의 마일스톤 구분에 맞춰 두 단계로 나눈다.

### 목표 — M1 핵심 MVP (Phase 0~8)
- 이메일 회원가입/로그인과 구글 소셜 로그인을 지원하는 안전한 인증 체계
- 할 일 생성·조회·수정·완료·삭제의 전 과정이 매끄럽게 동작
- 목록의 페이지네이션·필터·정렬
- 삭제 데이터의 **유실 방지**(Soft Delete) — 복구용 API·화면은 이번 범위 밖이며, 필요 시 DB에서 직접 되돌린다
- 뉴트럴 미니멀 디자인과 다크/라이트 모드, 반응형 레이아웃

### 목표 — M2 확장 기능 (Phase 9~11)
- 비밀번호를 잊은 사용자가 스스로 복구할 수 있는 재설정 흐름
- 할 일에 참고 자료를 파일로 첨부
- 관리자가 사용자와 전체 현황을 파악하고 문제 계정을 차단할 수 있는 관리 기능

### 목표 — M3 운영 배포 (Phase 12)
- 로컬에서 완성 후 코드 수정 없이 AWS로 이전 가능한 구조

### 비목표 (이번 범위에서 제외)
- 팀/조직 기능, 할 일 공유 및 협업
- 이메일·푸시 알림, 리마인더 (비밀번호 재설정 메일은 예외)
- **반복 일정** (매일/매주 자동 생성)
- 본문(Tiptap) 내 이미지 삽입 — 첨부파일은 별도 영역으로만 지원
- 하위 할 일(체크리스트), 캘린더 뷰
- 회원가입 시 이메일 인증
- 통계 대시보드의 고도화(차트, 기간별 추이) — 요약 수치만 제공
- 다국어 지원, 모바일 앱

---

## 3. 타깃 사용자

| 구분 | 설명 |
|---|---|
| 주 사용자 | 개인 할 일을 웹에서 관리하려는 일반 사용자 (role: USER) |
| 관리자 | 서비스 운영자. 사용자 현황 파악과 문제 계정 차단을 담당 (role: ADMIN) |
| 사용 환경 | 데스크톱 브라우저 중심, 모바일 브라우저 지원 |
| 전제 | 각 사용자는 자신의 데이터만 접근한다. 사용자 간 데이터 공유는 없다 |

---

## 4. 사용자 스토리

| ID | 스토리 |
|---|---|
| US-01 | 사용자로서 이메일과 비밀번호로 가입해 내 계정을 만들고 싶다 |
| US-02 | 사용자로서 구글 계정으로 간편하게 로그인하고 싶다 |
| US-03 | 사용자로서 로그인해야만 내 할 일에 접근되기를 원한다 |
| US-04 | 사용자로서 제목과 상세 내용, 마감일, 우선순위를 지정해 할 일을 등록하고 싶다 |
| US-05 | 사용자로서 상세 내용을 굵게·목록 등으로 서식을 넣어 작성하고 싶다 |
| US-06 | 사용자로서 할 일 목록을 최신순 또는 마감일순으로 보고 싶다 |
| US-07 | 사용자로서 완료/미완료로 걸러서 보고 싶다 |
| US-08 | 사용자로서 목록이 길어져도 페이지 단위로 편하게 탐색하고 싶다 |
| US-09 | 사용자로서 체크박스를 누르면 기다림 없이 즉시 완료 표시되기를 원한다 |
| US-10 | 사용자로서 할 일을 삭제해도 데이터가 곧바로 완전히 사라지지는 않기를 원한다 |
| US-11 | 사용자로서 눈이 편한 다크 모드와 밝은 라이트 모드를 골라 쓰고 싶다 |
| US-12 | 사용자로서 휴대폰에서도 레이아웃이 깨지지 않고 사용하고 싶다 |
| US-13 | 사용자로서 비밀번호를 잊었을 때 이메일로 재설정 링크를 받아 스스로 복구하고 싶다 |
| US-14 | 사용자로서 할 일에 참고 파일을 첨부하고 나중에 내려받고 싶다 |
| US-15 | 관리자로서 가입한 사용자 목록과 전체 현황을 확인하고 싶다 |
| US-16 | 관리자로서 문제가 되는 계정을 비활성화해 접근을 막고 싶다 |
| US-17 | 서비스 운영자로서 일반 사용자가 관리 기능에 접근할 수 없다는 것을 보장받고 싶다 |

---

## 5. 사용자 여정

화면 간 이동과 자동 리디렉션 조건을 한눈에 보기 위한 흐름도다. 각 화면의 상세는 8장을 참조한다.

```
[최초 진입]
   │
   ├─ 토큰 없음 ─────────────────► 로그인
   │                                 │
   │                                 ├─ 이메일 로그인 ────────────────┐
   │                                 │                                │
   │                                 ├─ 구글 로그인 ──► OAuth 콜백 ───┤
   │                                 │                (코드 → JWT)     │
   │                                 │                                │
   │                                 ├─ 회원가입 ──► 가입 성공 ────────┤
   │                                 │              (자동 로그인)      │
   │                                 │                                │
   │                                 └─ 비밀번호 찾기                  │
   │                                        │                         │
   │                                        ▼                         │
   │                                   재설정 요청                     │
   │                                        │ (메일·로그의 링크)        │
   │                                        ▼                         │
   │                                   비밀번호 변경 ──► 로그인으로 복귀 │
   │                                                                  │
   └─ 유효한 토큰 ───────────────────────────────────────────────────►┤
                                                                      ▼
                                                                 할 일 목록
                                                                      │
              ┌──────────────┬──────────────┬──────────────┬──────────┴──────┐
              ▼              ▼              ▼              ▼                 ▼
        생성 다이얼로그  수정 다이얼로그  완료 토글      필터·정렬·페이지   (ADMIN만)
              │              │          (낙관적)            │            관리자
              └──── 저장 ────┴──────────────┴──► 목록 갱신 ◄─┘                │
                                                                    (USER 접근 시)
                                                                            ▼
                                                                     접근 불가 안내
```

### 전환 조건

| 상황 | 동작 | 근거 |
|---|---|---|
| 토큰 없이 보호 화면 접근 | 로그인으로 리디렉션 | FR-A10 |
| 토큰 만료·무효(401 수신) | 토큰 삭제 → 로그인 이동 + 만료 안내 표시 | FR-A06 |
| 회원가입 성공 | 자동 로그인 → 할 일 목록 | FR-A04 |
| 구글 로그인 성공 | 콜백에서 일회용 코드 교환 → 할 일 목록. 실패 시 로그인(에러 표시) | FR-A08 |
| 구글 계정 이메일이 기존 계정과 동일 | 새 계정을 만들지 않고 기존 계정에 연결 | FR-A09 |
| USER가 관리자 화면 접근 | 접근 불가 안내 화면 | FR-M10 |
| 비활성화된 계정의 로그인·기존 토큰 사용 | 모두 차단 | FR-M06 |
| 필터·정렬 변경 | 첫 페이지로 이동 | FR-L06 |
| 재설정 요청 (가입 여부 무관) | 항상 동일한 성공 안내 | FR-R03 |

---

## 6. 기능 요구사항

### 6.1 인증 (AUTH)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-A01 | 회원가입 ID는 **이메일만** 사용한다. 이메일 형식을 검증한다 | 필수 |
| FR-A02 | 비밀번호는 **6자 이상**이며 BCrypt(strength 10)로 해싱해 저장한다 | 필수 |
| FR-A03 | 이미 존재하는 이메일로 가입 시 409와 안내 메시지를 반환한다 | 필수 |
| FR-A04 | 로그인 성공 시 **JWT Access Token(24시간 만료)** 을 발급한다. Refresh Token은 사용하지 않는다 | 필수 |
| FR-A05 | 토큰은 클라이언트 **localStorage**에 저장하고 모든 요청에 `Authorization: Bearer` 헤더로 전송한다 | 필수 |
| FR-A06 | 토큰 만료·무효 시 401을 반환하고, 프론트는 토큰을 삭제한 뒤 로그인 화면으로 이동하며 만료 안내를 표시한다 | 필수 |
| FR-A07 | **구글 OAuth2 로그인**을 지원한다. 백엔드 리다이렉트 방식으로 처리한다 | 필수 |
| FR-A08 | OAuth 성공 시 JWT를 URL에 직접 노출하지 않고, 단기(1~2분) 일회용 코드를 프론트에 전달한 뒤 교환 API로 JWT를 발급한다. 코드는 1회만 사용 가능하다. 코드는 `oauth_auth_codes` 테이블에 **해시로 저장**하며(`password_reset_tokens`와 같은 패턴), 재사용 여부를 서버에서 판정한다 | 필수 |
| FR-A09 | 구글 계정의 이메일이 기존 계정과 같으면 **기존 계정에 연결**한다. 새 계정을 만들지 않는다 | 필수 |
| FR-A10 | 인증되지 않은 사용자는 할 일 관련 모든 API와 화면에 접근할 수 없다 | 필수 |

### 6.2 할 일 관리 (TODO)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-T01 | 할 일은 제목(필수, 1~200자), 내용(선택, 서식 있는 텍스트), 마감일(선택), 우선순위(HIGH/MEDIUM/LOW, 기본 MEDIUM), 완료여부를 가진다 | 필수 |
| FR-T02 | 내용은 **Tiptap 에디터**로 작성한다. 툴바는 굵게·기울임·밑줄·취소선·목록·제목·인용·링크로 제한한다 | 필수 |
| FR-T03 | 내용 HTML은 **서버에서 정제(sanitize)** 한 뒤 저장한다. script 태그, on* 이벤트 속성, javascript: 링크는 제거한다. **jsoup의 `Safelist.relaxed()`를 그대로 쓰지 않고 FR-T02 툴바와 1:1 대응하는 커스텀 Safelist를 정의한다** — 취소선 `<s>`를 반드시 허용(relaxed는 `strike`만 허용해 서식이 소실됨)하고, 비목표인 `<img>`·`<table>`·`<div>`는 제외하며, `<a>`에 `rel`·`target`을 허용한다. `preserveRelativeLinks`는 XSS 우회 위험이 있어 켜지 않는다 | 필수 |
| FR-T04 | 할 일의 생성·조회·수정·삭제가 가능하다 | 필수 |
| FR-T05 | 완료 상태는 체크박스로 토글한다 | 필수 |
| FR-T06 | **삭제는 Soft Delete**로 처리한다. 물리 삭제하지 않고 `deleted_at`을 기록하며, 이후 모든 조회에서 제외한다 | 필수 |
| FR-T07 | 모든 단건 접근(조회/수정/토글/삭제)에서 **소유권을 검증**한다. 타인의 리소스 접근 시 404를 반환한다(존재 여부를 노출하지 않기 위해 403을 쓰지 않는다). 단 **역할 기반 접근 거부(FR-M02)는 403으로 구분**한다 — 소유권과 권한은 다른 층위다 | 필수 |

### 6.3 목록·탐색 (LIST)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-L01 | 목록은 서버 페이지네이션으로 제공한다. 기본 페이지 크기 **10**, 기본 정렬 **최신순(createdAt desc)** | 필수 |
| FR-L02 | 응답에 현재 페이지, 페이지 크기, 전체 건수, 전체 페이지 수, 첫/마지막 페이지 여부를 포함한다 | 필수 |
| FR-L03 | 상태 필터를 제공한다: 전체(기본) / 미완료 / 완료 | 필수 |
| FR-L04 | 정렬은 최신순 / 마감일순을 제공한다 | 필수 |
| FR-L05 | **재사용 가능한 페이지네이션 컴포넌트**를 별도로 구현한다. 페이지 번호, 이전/다음, 생략(…) 처리, 현재 페이지 강조, 모바일 축약형을 지원한다 | 필수 |
| FR-L06 | 필터·정렬·페이지 상태가 서로 충돌 없이 동작하며, 필터 변경 시 첫 페이지로 이동한다. **삭제·토글로 현재 페이지가 비면 이전 페이지로 이동**한다(마지막 페이지의 마지막 항목을 지웠을 때 빈 페이지에 갇히지 않는다) | 필수 |

### 6.4 UI/UX

> 이 그룹은 특정 화면 하나에 속하지 않는 **전 화면 공통 요구사항**이다. 8.1 "전 화면 공통 사항" 참조.

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-U01 | 디자인 방향은 **뉴트럴 미니멀**이다. 무채색 스케일 + 액센트 컬러 1개, 헤어라인 보더, 넉넉한 여백, 타이포 중심 위계 | 필수 |
| FR-U02 | **다크 모드를 기본**으로 하고 라이트 모드 토글을 제공한다. 두 모드 모두 텍스트 대비가 충분해야 한다 | 필수 |
| FR-U03 | 그라데이션·글로우·과한 그림자는 사용하지 않는다 | 필수 |
| FR-U04 | 모든 목록 화면은 **로딩(스켈레톤) / 빈 상태 / 에러 상태** UI를 갖춘다 | 필수 |
| FR-U05 | 완료 토글과 삭제는 **낙관적 업데이트**로 즉시 반영하고, 실패 시 원래 상태로 롤백하며 에러 토스트를 표시한다. **현재 필터 조건을 벗어나게 되는 변경은 필드 수정이 아니라 목록에서의 제거로 처리한다** (예: 미완료 필터에서 완료로 토글하면 항목이 목록에서 사라진다) | 필수 |
| FR-U06 | 애니메이션은 목록 진입/삭제/완료 토글 등 상태 변화에만 절제해서 사용한다 | 권장 |
| FR-U07 | 반응형: 모바일 1열 카드형, 데스크톱 리스트형. 모바일에서 레이아웃이 깨지지 않는다 | 필수 |
| FR-U08 | 에러 메시지는 무슨 일이 있었는지와 다음에 무엇을 할지를 한 문장으로 안내한다. 서버 스택트레이스를 노출하지 않는다 | 필수 |

### 6.5 비밀번호 재설정 (RESET)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-R01 | 이메일을 입력하면 재설정 링크를 메일로 발송한다. 로컬 개발에서는 링크를 로그로 출력한다 | 필수 |
| FR-R02 | 토큰은 30분 만료, 1회용이며 DB에는 해시로 저장한다 | 필수 |
| FR-R03 | 가입되지 않은 이메일이나 소셜 전용 계정이어도 **동일한 성공 응답**을 반환한다(계정 존재 여부 노출 금지) | 필수 |
| FR-R04 | 재설정 시 새 비밀번호는 6자 이상이며 확인 입력과 일치해야 한다 | 필수 |
| FR-R05 | 재설정 성공 시 해당 사용자의 미사용 재설정 토큰을 모두 무효화하고, **`credentials_changed_at`을 갱신해 그 이전에 발급된 JWT도 함께 무효화**한다(비밀번호를 바꿔도 탈취된 토큰이 24시간 살아있는 것을 막는다) | 필수 |
| FR-R06 | 만료·재사용·위조 토큰은 "유효하지 않거나 만료된 링크입니다"로 동일하게 처리한다 | 필수 |
| FR-R07 | 동일 이메일의 연속 요청을 제한한다(1분 내 재요청 차단) | 권장 |

### 6.6 파일 첨부 (FILE)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-F01 | 할 일 하나에 최대 **5개**, 개당 최대 **10MB**까지 첨부할 수 있다. Spring Boot 기본 한도(`max-file-size` 1MB)로는 실패하므로 `spring.servlet.multipart.*`를 반드시 상향한다 | 필수 |
| FR-F02 | 허용 형식: jpg, png, webp, gif, pdf, docx, xlsx, txt, zip. 확장자와 Content-Type을 모두 검증한다. 둘 다 위조 가능하므로 **실제 방어선은 파일 시그니처(매직바이트) 검사**이며, 업로드 파일은 서버에서 실행되지 않고 다운로드 전용으로만 다룬다 | 필수 |
| FR-F03 | 저장소는 인터페이스로 추상화한다. 로컬 개발은 디스크, AWS는 S3를 사용하며 환경변수로 전환한다 | 필수 |
| FR-F04 | 저장 키는 UUID 기반으로 생성하고 원본 파일명은 DB에만 보관한다 | 필수 |
| FR-F05 | 다운로드는 **소유권을 검증한 요청만 허용**하고, 저장소(디스크·버킷)로의 직접 접근은 차단한다. 구현 수단은 `FileStorage`가 담당하며 환경별로 다를 수 있다 — presigned URL은 S3 고유 기능이라 로컬 디스크에 대응물이 없으므로 요구사항에 못박지 않는다(구현 방식은 OPEN-06) | 필수 |
| FR-F06 | 첨부 삭제는 Soft Delete로 처리하며 실제 파일은 즉시 지우지 않는다 | 필수 |
| FR-F07 | 업로드 UI는 드래그앤드롭과 파일 선택을 지원하고, 진행 상태·파일명·크기를 표시한다. 이미지에는 썸네일을 보여준다 | 필수 |
| FR-F08 | 목록에서 첨부가 있는 할 일에 클립 아이콘과 개수를 표시한다 | 권장 |

> 본문(Tiptap) 내 이미지 삽입은 지원하지 않는다 — 2장 비목표 참조.

### 6.7 관리자 (ADMIN)

| ID | 요구사항 | 우선순위 |
|---|---|---|
| FR-M01 | 사용자는 `USER` 또는 `ADMIN` 권한을 가지며, JWT에 권한 정보를 포함한다 | 필수 |
| FR-M02 | **관리자 API는 서버에서 권한을 검사한다.** SecurityConfig와 `@PreAuthorize`를 이중으로 적용하며, 프론트 화면 가드는 보안 수단으로 인정하지 않는다 | 필수 |
| FR-M03 | 최초 관리자는 SQL로 직접 지정한다. 회원가입 등 일반 경로로 ADMIN이 될 수 없다 | 필수 |
| FR-M04 | 관리자는 사용자 목록을 페이지네이션으로 조회하고 이메일로 검색할 수 있다 | 필수 |
| FR-M05 | 관리자는 계정을 활성/비활성으로 전환할 수 있다. 자기 자신은 비활성화할 수 없다 | 필수 |
| FR-M06 | 비활성화된 계정은 로그인이 차단되며, 이미 발급된 토큰으로도 접근할 수 없다. JWT는 stateless이므로 **인증 필터가 매 요청마다 사용자 상태(`enabled`, `deleted_at`, `credentials_changed_at`)를 조회해 검증**한다. 이 조회 지점은 Phase 2에서 미리 마련하고 Phase 11에서 `enabled` 판정만 얹는다 | 필수 |
| FR-M07 | 관리자는 전체 할 일을 작성자와 함께 페이지네이션으로 조회할 수 있다 | 필수 |
| FR-M08 | 관리자는 요약 통계(전체 사용자 수, 활성 사용자 수, 전체 할 일 수, 완료 수)를 볼 수 있다 | 필수 |
| FR-M09 | 관리자 응답에 비밀번호 해시 등 민감정보를 포함하지 않는다 | 필수 |
| FR-M10 | 일반 사용자가 `/admin`에 직접 접근하면 접근 불가 안내 화면을 표시한다 | 필수 |

---

## 7. 메뉴 구조

권한별로 무엇이 보이는지를 정의한다. 모든 항목은 8장에 대응 화면이 존재한다.

```
공통 헤더
├── 로고 ──────────────► 할 일 목록 (비로그인 시 로그인)
├── 테마 토글 ─────────► FR-U02
└── 사용자 메뉴
    ├── [비로그인]  로그인 ────────► FR-A04, FR-A07
    │                회원가입 ──────► FR-A01
    ├── [USER]      로그아웃 ──────► FR-A05 (토큰 삭제)
    └── [ADMIN]     관리자 ────────► FR-M04, FR-M07, FR-M08
                     로그아웃

할 일 목록 화면 내부 컨트롤
├── 상태 필터   전체 / 미완료 / 완료 ──► FR-L03
├── 정렬        최신순 / 마감일순 ─────► FR-L04
├── 페이지네이션 ────────────────────► FR-L01, FR-L05
└── 새 할 일 버튼 ──► 생성 다이얼로그 ──► FR-T01, FR-T04

로그인 화면 내부 링크
├── 회원가입으로 ─────────────────► FR-A01
└── 비밀번호를 잊으셨나요 ──────────► FR-R01

관리자 화면 탭
├── 요약 통계 ────────────────────► FR-M08
├── 사용자 목록 (검색 · 활성/비활성) ─► FR-M04, FR-M05
└── 전체 할 일 ───────────────────► FR-M07
```

---

## 8. 화면 정의와 페이지별 상세 기능

### 8.0 화면 목록

| 라우트 | 화면 | 구현 요구사항 | 인증 |
|---|---|---|---|
| `/login` | 로그인 | FR-A04, A07 | X |
| `/signup` | 회원가입 | FR-A01, A02, A03 | X |
| `/oauth/callback` | OAuth 콜백 | FR-A08 | X |
| `/todos` | 할 일 목록(메인) | FR-T04~T06, FR-L01~L06, FR-U05, FR-F08 | O |
| `/todos` 내 다이얼로그 | 생성·수정 | FR-T01, T02, T04, FR-F01, F02, F05, F07 | O |
| `/forgot-password` | 재설정 요청 | FR-R01, R03, R07 | X |
| `/reset-password` | 비밀번호 변경 | FR-R04, R06 | X |
| `/admin` | 관리자 | FR-M04, M05, M07, M08 | ADMIN |
| `/admin` 접근 실패 시 | 접근 불가 안내 | FR-M10 | O (USER) |

### 8.1 전 화면 공통 사항

특정 화면 하나에 속하지 않고 모든 화면에 적용되는 요구사항이다.

| 항목 | 내용 | 구현 요구사항 |
|---|---|---|
| **디자인** | 뉴트럴 미니멀. 무채색 + 액센트 1개, 헤어라인 보더, 그라데이션·글로우 금지 | FR-U01, FR-U03 |
| **테마** | 다크 기본 + 라이트 토글. 두 모드 모두 텍스트 대비 확보 | FR-U02 |
| **반응형** | 모바일 1열 카드형, 데스크톱 리스트형 | FR-U07 |
| **상태 UI** | 목록이 있는 모든 화면에 로딩 스켈레톤 / 빈 상태 / 에러 상태 | FR-U04 |
| **에러 표시** | 무슨 일이 있었는지 + 다음에 할 일을 한 문장으로. 스택트레이스 노출 금지 | FR-U08 |
| **토큰 처리** | localStorage 저장, 모든 요청에 `Authorization: Bearer` 부착 | FR-A05 |
| **401 처리** | 토큰 삭제 → 로그인 이동 + 만료 안내 | FR-A06 |
| **라우트 가드** | 미인증 시 보호 화면 접근 차단. localStorage는 서버에서 읽을 수 없고 Next.js 16에서 `middleware.js`는 `proxy.js`로 대체되었으므로 **서버 가드를 쓸 수 없다** — 가드는 클라이언트 전용이며, 보호 화면은 `'use client'`로 두고 **인증 판정 전 로딩 상태**를 반드시 표시한다(로그인 화면이 번쩍이는 것을 막는다) | FR-A10, FR-A05 |
| **애니메이션** | 상태 변화(진입·삭제·토글)에만 절제해서 사용 | FR-U06 |

---

### 로그인 (`/login`)

> **구현 요구사항:** `FR-A04`, `FR-A07` | **인증:** 불필요

| 항목 | 내용 |
|---|---|
| **역할** | 서비스의 진입 화면. 이메일과 구글 두 경로의 인증을 모두 받는다 |
| **진입 경로** | 최초 방문 · 미인증 상태로 보호 화면 접근 시 리디렉션 · 401 발생 시 자동 이동 · 로그아웃 후 |
| **사용자 행동** | 이메일·비밀번호 입력 후 제출, 또는 구글 로그인 버튼 클릭. 계정이 없으면 회원가입으로, 비밀번호를 잊었으면 재설정 요청으로 이동 |
| **주요 기능** | • 이메일·비밀번호 입력과 형식 검증<br>• 로그인 실패 시 에러 메시지<br>• 만료로 튕겨 온 경우 만료 안내 표시<br>• **구글로 로그인** 버튼<br>• 회원가입 링크 · 비밀번호 찾기 링크 |
| **다음 이동** | 성공 → 할 일 목록 · 구글 → OAuth 콜백 · 회원가입 → 회원가입 · 비밀번호 찾기 → 재설정 요청 |

---

### 회원가입 (`/signup`)

> **구현 요구사항:** `FR-A01`, `FR-A02`, `FR-A03` | **인증:** 불필요

| 항목 | 내용 |
|---|---|
| **역할** | 이메일 기반 신규 계정 생성 전용 화면 |
| **진입 경로** | 로그인 화면의 회원가입 링크 |
| **사용자 행동** | 이메일·비밀번호·이름을 입력하고 제출한다 |
| **주요 기능** | • 이메일 형식 검증<br>• 비밀번호 6자 이상 검증<br>• 이메일 중복(409) 시 안내 메시지<br>• 필드별 검증 메시지 표시<br>• **가입하기** 버튼 |
| **다음 이동** | 성공 → 자동 로그인 → 할 일 목록 · 실패 → 같은 화면에 에러 표시 |

---

### OAuth 콜백 (`/oauth/callback`)

> **구현 요구사항:** `FR-A08` | **인증:** 불필요 (교환 직전 단계)

| 항목 | 내용 |
|---|---|
| **역할** | 구글 인증 후 돌아오는 중간 화면. 일회용 코드를 JWT로 교환하는 것만 담당한다 |
| **진입 경로** | 로그인 화면의 구글 버튼 → 구글 인증 → 백엔드 리다이렉트 |
| **사용자 행동** | 없음. 자동으로 처리되며 사용자는 로딩만 본다 |
| **주요 기능** | • URL의 일회용 코드 추출<br>• 교환 API 호출 후 JWT 저장<br>• 로딩 표시<br>• 코드 만료·재사용·누락 시 에러 처리 |
| **다음 이동** | 성공 → 할 일 목록 · 실패 → 로그인(에러 표시) |

---

### 할 일 목록 (`/todos`) — 메인 화면

> **구현 요구사항:** `FR-T04`, `FR-T05`, `FR-T06`, `FR-L01`~`FR-L06`, `FR-U05`, `FR-F08` | **인증:** 필수

| 항목 | 내용 |
|---|---|
| **역할** | 로그인 후의 기본 화면. 할 일의 조회·완료·삭제와 목록 탐색이 모두 여기서 일어난다 |
| **진입 경로** | 로그인·가입·OAuth 성공 후 자동 이동 · 헤더 로고 클릭 · 다이얼로그 저장 후 복귀 |
| **사용자 행동** | 목록을 훑고, 체크박스로 완료를 토글하고, 항목을 눌러 수정하고, 필터·정렬·페이지를 바꾸며 탐색한다 |
| **주요 기능** | • 상태 필터(전체/미완료/완료)<br>• 정렬(최신순/마감일순)<br>• 페이지네이션 10건 · 생략(…) 처리 · 모바일 축약형<br>• 낙관적 완료 토글, 실패 시 롤백 + 에러 토스트<br>• 낙관적 Soft Delete<br>• 첨부가 있으면 클립 아이콘과 개수 표시<br>• 로딩 스켈레톤 / 빈 상태 / 에러 상태<br>• **새 할 일** 버튼 |
| **다음 이동** | 생성·수정 다이얼로그(같은 화면 위) · 401 발생 시 로그인 |

---

### 생성·수정 다이얼로그 (`/todos` 내부)

> **구현 요구사항:** `FR-T01`, `FR-T02`, `FR-T04`, `FR-F01`, `FR-F02`, `FR-F05`, `FR-F07` | **인증:** 필수

| 항목 | 내용 |
|---|---|
| **역할** | 할 일의 입력과 편집, 그리고 첨부파일 관리를 담당하는 단일 입력 지점 |
| **진입 경로** | 목록의 **새 할 일** 버튼(생성) · 목록 항목 클릭(수정) |
| **사용자 행동** | 제목을 쓰고, 필요하면 Tiptap으로 내용에 서식을 넣고, 마감일·우선순위를 고르고, 파일을 끌어다 놓은 뒤 저장한다 |
| **주요 기능** | • 제목 입력(1~200자 필수)<br>• Tiptap 에디터 — 굵게·기울임·밑줄·취소선·목록·제목·인용·링크로 툴바 제한<br>• 마감일 선택(선택 항목)<br>• 우선순위 선택(HIGH/MEDIUM/LOW, 기본 MEDIUM)<br>• 첨부 드래그앤드롭 + 파일 선택, 진행 상태·파일명·크기 표시, 이미지 썸네일<br>• 첨부 최대 5개 · 개당 10MB · 허용 형식 검증<br>• 첨부 다운로드(단기 만료 URL) 및 삭제<br>• **저장** / **취소** 버튼 |
| **다음 이동** | 저장 → 다이얼로그 닫고 목록 갱신 · 검증 실패 → 필드별 에러 표시 |

> 본문 내 이미지 삽입은 지원하지 않는다. 첨부는 별도 영역으로만 다룬다.

---

### 재설정 요청 (`/forgot-password`)

> **구현 요구사항:** `FR-R01`, `FR-R03`, `FR-R07` | **인증:** 불필요

| 항목 | 내용 |
|---|---|
| **역할** | 비밀번호를 잊은 사용자가 재설정 링크를 요청하는 화면 |
| **진입 경로** | 로그인 화면의 "비밀번호를 잊으셨나요" 링크 |
| **사용자 행동** | 이메일을 입력하고 제출한 뒤 메일함을 확인한다 |
| **주요 기능** | • 이메일 입력과 형식 검증<br>• 가입 여부·소셜 전용 여부와 무관하게 **항상 동일한 안내 문구** 표시<br>• 1분 내 재요청 차단 안내<br>• **재설정 링크 받기** 버튼 |
| **다음 이동** | 제출 → 같은 화면에 안내 문구 표시 · 메일(로컬은 로그)의 링크 → 비밀번호 변경 |

---

### 비밀번호 변경 (`/reset-password`)

> **구현 요구사항:** `FR-R04`, `FR-R06` | **인증:** 불필요 (토큰으로 검증)

| 항목 | 내용 |
|---|---|
| **역할** | 재설정 토큰을 검증하고 새 비밀번호를 받는 화면 |
| **진입 경로** | 재설정 메일(로컬은 로그)의 링크. URL에 토큰이 포함된다 |
| **사용자 행동** | 새 비밀번호와 확인 비밀번호를 입력하고 제출한다 |
| **주요 기능** | • 진입 시 토큰 유효성 확인<br>• 새 비밀번호 6자 이상 검증<br>• 확인 입력 일치 검증<br>• 만료·재사용·위조 토큰을 "유효하지 않거나 만료된 링크입니다"로 동일 처리<br>• **비밀번호 변경** 버튼 |
| **다음 이동** | 성공 → 로그인(새 비밀번호로 로그인) · 토큰 무효 → 재설정 요청으로 안내 |

---

### 관리자 (`/admin`)

> **구현 요구사항:** `FR-M04`, `FR-M05`, `FR-M07`, `FR-M08` | **인증:** ADMIN 권한 필수

| 항목 | 내용 |
|---|---|
| **역할** | 서비스 운영 현황 파악과 문제 계정 차단을 담당하는 관리 전용 화면 |
| **진입 경로** | ADMIN 권한으로 로그인한 뒤 헤더의 관리자 메뉴 |
| **사용자 행동** | 요약 수치를 확인하고, 이메일로 사용자를 찾아 계정을 활성/비활성으로 바꾸고, 전체 할 일을 열람한다 |
| **주요 기능** | • 요약 통계 카드 — 전체 사용자 수 · 활성 사용자 수 · 전체 할 일 수 · 완료 수<br>• 사용자 목록 탭 — 페이지네이션 + 이메일 검색<br>• 계정 활성/비활성 토글 (자기 자신은 불가)<br>• 전체 할 일 탭 — 작성자와 함께 페이지네이션 조회<br>• 로딩 / 빈 / 에러 상태 |
| **다음 이동** | 탭 전환(같은 화면) · 로그아웃 → 로그인 |

---

### 접근 불가 안내

> **구현 요구사항:** `FR-M10` | **인증:** 로그인은 되어 있으나 권한 없음(USER)

| 항목 | 내용 |
|---|---|
| **역할** | 권한이 없는 사용자가 관리자 화면에 접근했을 때 보여주는 안내 화면 |
| **진입 경로** | USER 권한으로 `/admin`에 직접 접근 |
| **사용자 행동** | 안내를 읽고 할 일 목록으로 돌아간다 |
| **주요 기능** | • 접근 권한이 없다는 안내 한 문장<br>• **할 일 목록으로 돌아가기** 버튼 |
| **다음 이동** | 할 일 목록 |

> 이 화면은 **편의를 위한 안내일 뿐 보안 수단이 아니다.** 실제 차단은 서버의 권한 검사(FR-M02)가 담당한다.

### 8.2 화면이 없는 요구사항 (서버·운영 전용)

정합성 검증 시 "고아 요구사항"으로 오인되지 않도록 명시한다.

| ID | 요구사항 | 담당 |
|---|---|---|
| FR-A02 | BCrypt 해싱 | 서버 |
| FR-A09 | 동일 이메일 계정 연결 | 서버 (OAuth 처리) |
| FR-T03 | HTML 정제 | 서버 |
| FR-T07 | 소유권 검증 (타인 리소스 404) | 서버 |
| FR-F03, F04, F06 | 저장소 추상화 · UUID 키 · 첨부 Soft Delete | 서버 |
| FR-R02, R05 | 토큰 해시 저장·만료 · 재설정 후 토큰 무효화 | 서버 |
| FR-M01, M02, M06, M09 | 권한 정보 · 이중 권한 검사 · 비활성 계정 차단 · 민감정보 제외 | 서버 |
| FR-M03 | 최초 관리자 지정 | 운영 절차 (SQL) |

---

## 9. 데이터 모델

개발용 DB는 database `postgres` 안의 **schema `todolist_db`**, 테스트용은 같은 database 안의 **schema `todolist_db_test`** 다. PostgreSQL의 *database*가 아니라 *schema*라는 점에 주의한다 — 접속 URL은 `jdbc:postgresql://localhost:5432/postgres?currentSchema=todolist_db` 형태다.

> **Hibernate의 `ddl-auto`는 테이블은 만들지만 스키마 자체는 만들지 않는다.** `CREATE SCHEMA IF NOT EXISTS todolist_db;`가 반드시 선행되어야 하며, 이는 `db/init.sql`이 담당한다.
>
> 공통 `application.properties`의 `hibernate.default_schema`는 모든 프로파일에 적용되므로, **테스트 프로파일에서 `todolist_db_test`로 반드시 오버라이드**한다. 이를 빠뜨리면 Phase 8에서 CI 통합테스트가 전부 실패한다.

이 장이 데이터 모델의 단일 출처다.

### users
`id`, `email`(unique), `password`(nullable — 소셜 전용 계정), `name`, `provider`(LOCAL/GOOGLE), `provider_id`, `role`(USER/ADMIN), `enabled`, `credentials_changed_at`, `created_at`, `updated_at`, `deleted_at`

- `credentials_changed_at`: 비밀번호 변경·계정 비활성화 시각. 이 시각 이전에 발급된 JWT를 무효화하는 데 쓴다(FR-R05, FR-M06)

### todos
`id`, `user_id`(FK), `title`, `content`(TEXT, 정제된 HTML), `completed`, `due_date`, `priority`(HIGH/MEDIUM/LOW), `created_at`, `updated_at`, `deleted_at`

### password_reset_tokens
`id`, `user_id`(FK), `token`(해시 저장), `expires_at`, `used_at`, `created_at`

### oauth_auth_codes
`id`, `user_id`(FK), `code`(해시 저장), `expires_at`, `used_at`, `created_at`

- FR-A08의 일회용 코드. `password_reset_tokens`와 같은 패턴이므로 구현을 재사용한다
- 인메모리(`ConcurrentHashMap`) 대신 테이블을 쓰는 이유: 서버 재시작과 다중 인스턴스에서도 1회용 보장이 유지된다

### attachments
`id`, `todo_id`(FK), `original_name`, `stored_key`, `content_type`, `size_bytes`, `created_at`, `deleted_at`

- 모든 테이블에 `created_at` / `updated_at` / `deleted_at`을 둔다.
- 인덱스는 **부분 인덱스**로 만든다. 실제 쿼리가 `WHERE user_id = ? AND deleted_at IS NULL ORDER BY created_at DESC LIMIT 10`이므로, 필터·정렬·페이지네이션이 인덱스 하나로 끝나고 삭제된 행은 인덱스에서 아예 빠진다.

```sql
CREATE INDEX idx_todos_user_created ON todos (user_id, created_at DESC) WHERE deleted_at IS NULL;
CREATE INDEX idx_todos_user_due     ON todos (user_id, due_date)        WHERE deleted_at IS NULL;
CREATE INDEX idx_attachments_todo   ON attachments (todo_id)            WHERE deleted_at IS NULL;
```

> `(due_date)` **단독 인덱스는 만들지 않는다.** 마감일순 정렬(FR-L04)도 항상 `user_id`로 먼저 좁혀지므로 선행 컬럼이 `due_date`인 인덱스는 선택되지 않는다.

---

## 10. API 요약

| 메서드 | 경로 | 설명 | 인증 |
|---|---|---|---|
| POST | `/api/auth/signup` | 회원가입 | X |
| POST | `/api/auth/login` | 로그인 (JWT 반환) | X |
| GET | `/api/auth/me` | 내 정보 | O |
| GET | `/oauth2/authorization/google` | 구글 로그인 시작 | X |
| POST | `/api/auth/oauth/exchange` | 일회용 코드 → JWT 교환 | X |
| GET | `/api/todos` | 목록 (page, size, status, sort) | O |
| POST | `/api/todos` | 생성 | O |
| GET | `/api/todos/{id}` | 단건 조회 | O |
| PUT | `/api/todos/{id}` | 수정 | O |
| PATCH | `/api/todos/{id}/toggle` | 완료 토글 | O |
| DELETE | `/api/todos/{id}` | Soft Delete | O |
| POST | `/api/auth/password/forgot` | 재설정 메일 요청 | X |
| POST | `/api/auth/password/reset` | 토큰으로 비밀번호 변경 | X |
| POST | `/api/todos/{id}/attachments` | 첨부 업로드 | O |
| GET | `/api/todos/{id}/attachments` | 첨부 목록 | O |
| GET | `/api/attachments/{id}/download` | 첨부 다운로드 (소유권 검증 후) | O |
| DELETE | `/api/attachments/{id}` | 첨부 Soft Delete | O |
| GET | `/api/admin/users` | 사용자 목록 | ADMIN |
| PATCH | `/api/admin/users/{id}/status` | 계정 활성/비활성 | ADMIN |
| GET | `/api/admin/todos` | 전체 할 일 조회 | ADMIN |
| GET | `/api/admin/stats` | 요약 통계 | ADMIN |
| GET | `/actuator/health` | 헬스체크 | X |

- 첨부 다운로드의 구현 방식(서버 스트리밍 통일 / 환경별 서명 URL)은 **OPEN-06**에서 확정한다. 서명 URL 방식을 택하면 그 URL을 서빙·검증하는 엔드포인트가 이 표에 추가되어야 한다.
- 응답 DTO만 반환하며 엔티티를 직접 노출하지 않는다.
- 에러 응답은 `timestamp / status / code / message / errors[]` 형식으로 통일한다.
- API 문서는 SpringDoc OpenAPI(Swagger UI)로 제공하고, 운영 환경에서는 비활성화한다.

---

## 11. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 보안 | 비밀번호 BCrypt 해싱, JWT HS256, 시크릿은 환경변수로만 주입, 모든 입력값 서버 검증, HTML 정제, 업로드 파일 형식·용량 검증, 관리자 API 서버 권한 검사, CORS는 지정된 출처만 허용(와일드카드 금지), 운영에서 Swagger 차단 |
| 데이터 | 물리 삭제 금지, 소유권 검증 필수, 운영 DB는 자동 백업 활성화 *(Phase 12-2에서 검증)*, S3 버킷 퍼블릭 액세스 차단 *(Phase 12-3)* |
| 성능 | 목록 조회는 페이지 단위(10건)로만 조회하며 전체 로딩하지 않는다. 9장의 **부분 인덱스**(`WHERE deleted_at IS NULL`)를 사용해 필터·정렬·페이지네이션이 인덱스 하나로 처리되게 한다 |
| 이식성 | URL·DB 접속정보·CORS 출처를 소스에 하드코딩하지 않고 환경변수로 분리하며, 파일 저장과 메일 발송은 인터페이스로 추상화해 값 교체만으로 AWS 이전이 가능해야 한다 |
| 운영 | 로그는 표준출력, `/actuator/health` 제공(`spring-boot-starter-actuator` 필요), 서버 재시작 시 자동 기동 *(Phase 12-4에서 검증)* |
| 접근성 | 다크·라이트 모두 충분한 텍스트 대비, 키보드로 주요 흐름(입력·제출·토글) 조작 가능 |
| 테스트 | 백엔드 통합테스트 작성(프론트 자동화 테스트는 범위 밖). Docker/Testcontainers 미사용 |

---

## 12. 제약 조건

- 기술 스택과 버전은 `CLAUDE.md` 2장에 고정되어 있으며 임의 변경할 수 없다.
- 저장소는 문서·프론트·백엔드 **3개로 분리**되어 있다(`CLAUDE.md` 1장). 커밋은 해당 저장소 안에서 실행한다.
- **Docker를 사용하지 않는다.** 통합테스트는 로컬 PostgreSQL의 `todolist_db_test` 스키마를 사용한다. 단 **CI 러너의 PostgreSQL 서비스 컨테이너는 예외** — 이 제약은 로컬 개발 환경을 대상으로 한다.
- 스키마 정의의 출처를 하나로 유지한다. `db/init.sql`은 스키마 생성·인덱스·시드를 담당하고 테이블 DDL은 Hibernate가 맡거나, 반대로 `ddl-auto=validate`로 통일한다. 둘 다 테이블을 정의하면 Phase 12-2의 `validate`에서 드리프트가 드러난다.
- 백엔드 베이스 패키지는 `com.example`, 빌드는 Maven.
- 개발은 전부 로컬에서 진행하고, AWS 작업은 모든 기능·테스트 완료 후에 착수한다.
- 파일 저장은 로컬 개발 시 디스크, AWS 이전 후 S3를 사용하며 코드 변경 없이 전환된다.
- 메일 발송은 로컬에서 로그 출력으로 대체하고, AWS 이전 시 SES로 교체한다.
- 모든 주석과 커밋 메시지는 한글로 작성한다.

---

## 13. 개발 순서

개발 Phase와 각 단계의 완료 조건은 [ROADMAP.md](ROADMAP.md)가 단일 출처다.

- 마일스톤 구분(M1 핵심 MVP / M2 확장 기능 / M3 운영 배포) — ROADMAP 2장
- Phase별 산출물·완료 조건·리스크 — ROADMAP 3장
- 요구사항 ID(FR-xxx)와 Phase의 대응 — ROADMAP 4장 "요구사항 추적 매트릭스"

---

## 14. 인수 기준

M1 게이트는 Phase 8 종료, M2 게이트는 Phase 11 종료 시점의 통과 조건이다.

> 각 항목은 **측정 가능한 형태**로 쓴다. "잘 동작한다" 같은 주관적 서술은 인수 기준이 될 수 없다.

### M1 게이트

| # | 기준 | 근거 |
|---|---|---|
| 1 | `postgres` DB에 `todolist_db` **스키마**가 생성되어 있고 `\dn`으로 확인된다 | 9장 |
| 2 | 백엔드가 오류 없이 기동 (`./mvnw spring-boot:run`) | — |
| 3 | 프론트엔드 빌드 성공 (`npm run build`) | — |
| 4 | 회원가입 시 사용자 생성 및 JWT 반환 | FR-A01~A03 |
| 5 | 로그인 시 유효한 JWT 반환 | FR-A04 |
| 6 | 보호된 엔드포인트가 유효한 토큰을 요구 (토큰 없으면 401) | FR-A10 |
| 7 | 할 일 CRUD가 페이지네이션과 함께 동작 | FR-T04, FR-L01 |
| 8 | Soft Delete 시 `deleted_at`이 갱신되고 목록에서 제외 | FR-T06 |
| 9 | 타인의 리소스 접근 시 404 | FR-T07 |
| 10 | 다크/라이트 토글이 동작하고, **두 모드 모두 본문 텍스트 대비가 4.5:1 이상**(브라우저 개발자도구 또는 대비 측정 도구로 확인) | FR-U02 |
| 11 | **320 / 768 / 1280px** 폭에서 가로 스크롤이 발생하지 않고 주요 컨트롤(필터·정렬·페이지네이션·생성 버튼)이 모두 조작 가능 | FR-U07 |
| 12 | 구글 로그인이 동작하고, 동일 이메일 계정이 중복 생성되지 않으며, **일회용 코드를 두 번째로 교환하면 실패**한다 | FR-A07~A09 |
| 13 | 내용에 `<script>` 태그, `on*` 이벤트 속성, `javascript:` 링크를 넣어 저장해도 **셋 다 정제**되어 저장됨 | FR-T03 |
| 14 | 만료된 토큰으로 요청 시 401을 받고, 프론트가 토큰을 삭제한 뒤 로그인 화면으로 이동하며 만료 안내를 표시 | FR-A06 |
| 15 | 페이지네이션이 첫·중간·마지막 페이지에서 모두 정상이며 생략(…) 처리와 모바일 축약형이 동작 | FR-L05 |
| 16 | 완료 토글 실패 시 원래 상태로 롤백되고 에러 토스트가 뜬다. 미완료 필터에서 완료로 토글하면 항목이 목록에서 사라진다 | FR-U05 |
| 17 | 마지막 페이지의 마지막 항목을 삭제하면 빈 페이지에 갇히지 않고 이전 페이지로 이동 | FR-L06 |

### M2 게이트

| # | 기준 | 근거 |
|---|---|---|
| 18 | 재설정 링크로 비밀번호 변경 후 새 비밀번호로 로그인 가능하며, 같은 토큰 재사용은 실패 | FR-R02, FR-R04 |
| 19 | 미가입 이메일로 재설정 요청 시에도 동일한 성공 응답 | FR-R03 |
| 20 | 동일 이메일로 1분 내 재요청 시 차단 | FR-R07 |
| 21 | 비밀번호 변경 **이전에 발급된 JWT**로 접근하면 401 | FR-R05 |
| 22 | 파일 업로드·다운로드·삭제가 동작하고, 용량·형식·개수 제한이 **서버에서** 적용됨 | FR-F01, FR-F02 |
| 23 | 타인의 할 일에 첨부 업로드 시 404 | FR-T07 |
| 24 | 첨부 다운로드가 소유권 검증을 거치며, 저장소(디스크·버킷) 직접 접근이 차단됨 | FR-F05 |
| 25 | USER 권한으로 `/api/admin/**` 접근 시 403, 토큰 없이 접근 시 401 | FR-M02 |
| 26 | 계정 비활성화 후 해당 계정의 로그인과 **기존 토큰 사용이 모두** 차단됨 | FR-M06 |
| 27 | 관리자가 자기 자신을 비활성화할 수 없음 | FR-M05 |

---

## 15. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| Spring Boot 4 / Security 7의 신규 문법을 도구가 구버전으로 작성 | 컴파일 실패, 잘못된 보안 설정 | `CLAUDE.md`에 버전 고정 + 구 API 금지 명시, 불확실하면 질문하도록 규칙화 |
| springdoc-openapi가 Spring Boot 4의 Jackson 3와 충돌 | **기동 실패** (`ClassNotFoundException: com.fasterxml.jackson.databind.node.ObjectNode`) | Spring Boot 4는 Jackson 3(`tools.jackson`)를 쓰지만 springdoc 3.x는 내부적으로 여전히 Jackson 2를 쓰며 그 의존성이 `provided` 스코프다. `springdoc-openapi-starter-webmvc-ui 3.1.x`와 함께 **`jackson-databind` 2.x를 명시적으로 추가**한다(패키지명이 달라 공존 가능). Phase 2에서 Swagger 추가 직후 기동을 먼저 확인한다 |
| AWS Amplify가 **Next.js 15까지만** SSR을 지원 (공식 문서 기준) | 배포 방식 변경 필요 | 이 앱은 localStorage 토큰(FR-A05) 때문에 서버 렌더링을 쓰지 않으므로 **정적 export가 자연스럽게 성립**한다(OPEN-01). Phase 5 시작 전에 `output: 'export'`를 적용해 처음부터 그 제약 안에서 개발한다 — Phase 12에 가서 전환하면 `<Suspense>` 경계 누락으로 빌드가 무더기로 깨진다 |
| localStorage 토큰의 XSS 노출 | 계정 탈취 | 사용자 입력 HTML 서버 정제 필수, 외부 스크립트 최소화 |
| 낙관적 업데이트와 페이지네이션의 상호작용 | 목록이 어긋나 보이거나 빈 페이지에 갇힘 | 쿼리 키에 필터·정렬·페이지를 포함하는 것만으로는 부족하다. ① 필터를 벗어나는 변경은 **목록에서 제거**(FR-U05), ② 삭제로 페이지가 비면 **이전 페이지로 이동**(FR-L06), ③ 페이지 경계에서 항목이 당겨오는 경우 invalidate로 재조회, ④ 롤백 스냅샷은 변경을 시작한 쿼리 키에 되돌린다 |
| 로컬 전용 코드가 AWS에서 깨짐 | 배포 지연 | 환경변수 분리 원칙(`CLAUDE.md` 5장), Phase 12-1 사전 점검 |
| 관리자 권한 검사를 프론트 가드에만 의존 | 권한 우회 | SecurityConfig + `@PreAuthorize` 이중 검사, 통합테스트로 403/401 검증 |
| 업로드 파일을 통한 악성 파일 유입 | 서버·사용자 피해 | 확장자·Content-Type 화이트리스트, 용량·개수 제한, UUID 키 저장, 버킷 퍼블릭 차단 |
| 재설정 응답으로 가입 여부가 드러남 | 계정 열거 공격 | 모든 경우 동일 응답, 요청 빈도 제한 |
| 로컬 디스크 저장이 EC2 재배포 시 소실 | 첨부 유실 | `FileStorage` 추상화로 AWS에서는 S3 사용 |
| **계정 선점(pre-hijacking)** — 이메일 인증 없는 가입(2장 비목표) + 이메일 기반 계정 연결(FR-A09) | 공격자가 피해자 이메일로 미리 로컬 가입해 두면, 피해자가 나중에 구글 로그인할 때 **공격자 계정에 연결**되어 계정을 공유하게 된다 | 대응 방식은 **OPEN-05**에서 확정한다. 현재는 리스크를 인지한 상태이며, 방치가 아니라 명시적 판단의 대상이다 |
| 공통 `application.properties`의 `hibernate.default_schema`가 테스트에도 적용됨 | Phase 8에서 CI 통합테스트 전량 실패 | 테스트 프로파일에서 `todolist_db_test`로 오버라이드하고, `db/init.sql`에 `CREATE SCHEMA IF NOT EXISTS`를 넣는다. `ddl-auto`는 스키마를 만들지 않는다 |
| Spring Security 7에서 제거된 API를 도구가 사용 | 컴파일 실패 | `.and()` · `authorizeRequests()` · `.apply()` · `AntPathRequestMatcher` · `MvcRequestMatcher`가 모두 제거되고 `PathPatternRequestMatcher`가 유일한 전략이다. `antMatchers`는 이미 Security 6에서 사라진 것이라 경계 대상이 아니다 |

---

## 16. 미결정 사항

### 16.1 결정이 필요한 항목

| ID | 항목 | 선택지 | 결정 시점 |
|---|---|---|---|
| OPEN-01 | 프론트 배포 방식 | **유력안: 정적 export(`output: 'export'`) + Amplify 정적 호스팅.** AWS 공식 문서상 Amplify의 SSR 지원은 Next.js 15까지이고 streaming도 미지원이지만, 이 앱은 localStorage 토큰(FR-A05)을 쓰므로 서버 렌더링이 필요 없다 | **Phase 5 시작 전** (뒤로 미루면 전환 비용이 커진다) |
| OPEN-02 | HTTPS 적용 방식 | Nginx + Let's Encrypt / ALB + ACM | Phase 12 |
| OPEN-03 | 도메인 | 자체 도메인 사용 / Amplify 기본 도메인 + EC2 퍼블릭 주소 | Phase 12 |
| OPEN-04 | 운영 메일 발송 | SES 샌드박스 해제 여부와 발신 도메인 검증 방식 | Phase 12 |
| **OPEN-05** | **계정 선점 대응** (15장 리스크) | ① 가입 시 이메일 인증 도입(2장 비목표와 충돌, Phase 9 메일 인프라 선행) / ② 구글 연결 시 기존 로컬 계정의 **비밀번호 확인** 요구 / ③ provider별 계정 분리, 연결은 로그인 상태에서만 허용 / ④ 개인 학습 프로젝트로서 **리스크를 명시적으로 수용** | **Phase 3 시작 전** |
| **OPEN-06** | **첨부 다운로드 구현 방식** (FR-F05) | ① 로컬에 HMAC 서명 URL + 검증 엔드포인트 구현(두 환경의 보안 특성이 동등해짐, 작업량 증가) / ② **두 환경 모두 서버 스트리밍으로 통일**(가장 단순·일관, 10MB×5 규모에서 부하 무시 가능) | **Phase 10 시작 전** |

> OPEN-03의 결정에 따라 구글 클라우드 콘솔의 승인된 리다이렉트 URI, `APP_CORS_ALLOWED_ORIGINS`, `APP_PASSWORD_RESET_URL` 값이 함께 달라진다.
>
> OPEN-01에서 정적 export를 택하면 **rewrites·redirects·headers·Intercepting Routes를 쓸 수 없다.** 프론트가 백엔드 오리진을 직접 호출하므로 CORS 설정이 필수가 되고, `useSearchParams`를 쓰는 화면(OAuth 콜백·비밀번호 변경·할 일 목록)은 `<Suspense>` 경계가 필요하다.

### 16.2 착수 전 확인이 필요한 기술 항목

아래는 **공식 문서로 확정하지 못한 사항**이다. 사실로 단정하지 않고, 해당 Phase 착수 시점에 실제로 확인한다.

| # | 확인할 것 | 확인 시점 |
|---|---|---|
| 1 | springdoc 3.1.x에서 Jackson 3 충돌이 해결되었는지. 넣고 기동해 보고, 실패하면 `jackson-databind` 2.x를 추가 | Phase 2 |
| 2 | Amplify의 Next.js 16 SSR 지원 현황 (Adapter API 협업 진행 중). OPEN-01에서 정적 export를 택하면 무관해진다 | Phase 12 |
| 3 | Tiptap의 React 19 / Next.js 16 지원. 공식 가이드가 지원 버전을 명시하지 않으므로, 설치 후 peer dependency 경고를 확인 | Phase 7 |
| 4 | Spring Boot 4 / Hibernate 7에서 `@SQLRestriction`의 동작과 count 쿼리 적용 범위. 전역 적용 시 FR-M07(관리자 전체 조회)에도 걸린다 | Phase 1 |
| 5 | Spring Boot 4의 분리된 `spring-boot-starter-*-test` 모듈만으로 `@SpringBootTest` 통합테스트에 필요한 것이 다 갖춰지는지 | Phase 8 |
| 6 | Spring Security 7의 OAuth2 Client DSL 구체 문법 | Phase 3 |

> **Tiptap은 `'use client'` + `immediatelyRender: false`가 필요하다**(SSR hydration 불일치 방지). 다이얼로그 전용이므로 `dynamic import`로 분리하는 편이 초기 번들에 유리하다.

---

## 17. 변경 이력

| 버전 | 일자 | 내용 |
|---|---|---|
| 1.0 | 2026-08-26 | 최초 작성 |
| 1.1 | 2026-08-27 | 사용자 여정·메뉴 구조·페이지별 상세 기능 추가. 개발 마일스톤을 ROADMAP으로 이관. 깨진 교차 참조(CLAUDE.md 장 번호, 없는 파일) 정정. "모노레포" 표기를 3개 저장소로 수정. FR-F09를 비목표로 흡수 |
| 1.2 | 2026-08-27 | 기술 검증 반영. `todolist_db`를 schema로 명확화, FR-M06에 매 요청 상태 조회 명시, FR-T03에 커스텀 Safelist 요구, FR-F05를 성질 중심으로 재작성, FR-A08 코드 저장 테이블 추가, 부분 인덱스로 교체, 인수 기준을 측정 가능하게 재작성(20→27개), 계정 선점 리스크와 OPEN-05·06 추가 |
